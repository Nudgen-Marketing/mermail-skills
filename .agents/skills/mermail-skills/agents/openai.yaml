interface:
  display_name: "Mermail Skills"
  short_description: "Repo-specific patterns and workflows for mermail-skills"
  default_prompt: "Use the mermail-skills repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true