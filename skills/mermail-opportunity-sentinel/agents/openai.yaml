interface:
  display_name: "Mermail Opportunity Sentinel"
  short_description: "Rank work offers and block job scams"
  default_prompt: "Use $mermail-opportunity-sentinel to review inbound job, freelance, and bounty email as a bounded, safety-first opportunity queue without replying or handling funds."
dependencies:
  tools:
    - type: "mcp"
      value: "mermail"
      description: "Mermail workspace and mailbox MCP server"
      transport: "streamable_http"
      url: "https://console.mermail.app/mcp"
