interface:
  display_name: "Mermail Autonomous Guardian"
  short_description: "Cryptographic task verification, anti-replay protection, and zero-trust Solana settlements for AI agents"
  default_prompt: "Use $mermail-autonomous-guardian to verify cryptographic task payloads, reject replay attacks, and execute bounded, simulated Solana settlements."
dependencies:
  tools:
    - type: "mcp"
      value: "mermail"
      description: "Mermail workspace and mailbox MCP server"
      transport: "streamable_http"
      url: "https://console.mermail.app/mcp"
